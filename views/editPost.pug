extends layouts/main.pug

block content
    .form-container
        form(action=`/board${routes.editPost}`,method="post")
            input(type="text", name="title",placeholder="title")
            textarea(name="description",placeholder="description")      
            input(type="submit",value="Edit Post")