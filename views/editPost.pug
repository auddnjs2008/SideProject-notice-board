extends layouts/main.pug

block content
    .form-container
        form(action=`${routes.editPost(post.id)}`,method="post")
            input(type="text", name="title",placeholder=post.title)
            textarea(name="description")=post.description      
            input(type="submit",value="Edit Post")