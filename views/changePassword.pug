extends layouts/main.pug

block content
    .form-container
        form(action=`/users${routes.changePassword}`,method="post")
            input(type="text",name="oldPassword",placeholder="oldPassword")
            input(type="text",name="newPassword",placeholder="newPassword")
            input(type="text",name="newPassword2",placeholder="newPassword")
            input(type="submit",value="change Password")